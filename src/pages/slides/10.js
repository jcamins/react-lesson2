import React from 'react';
import slide from '../../slide-hoc';

export default slide(() => (
    <section>
        <h1>Assignment: Setup</h1>
        <code><pre>
{`
git clone https://github.com/zocdoc/react-lesson2
cd react-lesson2
npm install
npm run develop
`}
        </pre></code>
    </section>
));